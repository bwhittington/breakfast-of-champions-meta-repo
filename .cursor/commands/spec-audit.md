---
name: "/spec-audit"
id: "spec-audit"
category: "Workflow"
description: "Adversarial audit — did shipped work match OpenSpec specs and tasks?"
---

Read-only adversarial audit of spec compliance.

**Load and follow:** `.cursor/skills/spec-adversarial-audit/SKILL.md`

**Input:** Optional change name, PR range, or `all` (default: all active OpenSpec changes + recent game PRs).

Do not implement fixes unless the operator asks to remediate after the report.
